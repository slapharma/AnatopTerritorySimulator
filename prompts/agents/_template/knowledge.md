<!--
TEMPLATE — optional. Extra standing questions for this agent, on top of questions.md.
Same format: a bolded topic, the question in plain English, and "Verify." on anything
that needs a search-backed tag. {{PRODUCT}}, {{INDICATION}} and {{COUNTRY}} are
substituted from the session's inputs, so keep these country-agnostic.

This file is the checked-in default and ships with the repo. The Knowledge box on the
Agents page overrides it for one deployment; clearing that box hands the agent back to
this file. Delete this file if the agent needs no extra questions.
-->

- **<Topic>.** <Question.> Verify.
